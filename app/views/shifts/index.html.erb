<%= render "shared/header" %>

<%= link_to 'シフトの追加', new_shift_path %>

<%= month_calendar shifts: @shifts do |date, shifts| %>
  <%= date.day %>
  <%# @shifts.each do |shift| %>
  <%= form_with model: @shift, url: shift_path, local: true do |f| %>
    <div>
      <%= link_to @shift.title %>
    </div>
  <% end %>
<% end %>
